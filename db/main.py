from db.models import micex_news
from db.queries.core import (clear_tables, create_tables, drop_tables,
                             insert_data)

# create_tables()
clear_tables(micex_news)
# drop_tables()
# insert_data()

